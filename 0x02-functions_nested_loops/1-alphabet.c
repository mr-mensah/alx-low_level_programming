#include "main.h"

/**
 * main - first entry point 
 *
 * Return: Alays 0(Success)
 */
void print_alphabet(void)
{
char alpha;
for (alpha = 'a'; alpha <= 'z'; alpha++)
_putchar(alpha);
_putchar('\n');
}
